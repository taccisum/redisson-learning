spring:
  profiles: getting-start
  redis:
    redisson:
      config: classpath:redisson.yaml
---
spring:
  profiles: getting-start
  redis:
    redisson:
      config: classpath:redisson/getting-start.yaml
